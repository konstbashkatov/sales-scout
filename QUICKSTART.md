# Sales Scout - Быстрый старт

## Статус: Развернуто на сервере! ✅

Sales Scout успешно развернут и запущен на сервере **aistudy.dev.o2it.ru:8100**

## Что сделано:

✅ Код развернут в `/opt/sales-scout`
✅ Виртуальное окружение создано
✅ Зависимости установлены
✅ Systemd сервис настроен и запущен
✅ Приложение работает на порту 8100
✅ Полная изоляция от других проектов

## Что нужно сделать:

### Шаг 1: Получите API ключи (если еще нет)

1. **DaData** → https://dadata.ru/ (регистрация, 10k запросов/день бесплатно)
2. **Perplexity** → https://www.perplexity.ai/ → dashboard
3. **OpenRouter** → у вас уже есть ключ

### Шаг 2: Заполните .env на сервере

```bash
ssh myserver
nano /opt/sales-scout/.env
```

Замените `your_*` на реальные значения:

```env
BITRIX24_WEBHOOK_URL=https://ваш-портал.bitrix24.ru/rest/1/ваш_код
BITRIX24_BOT_ID=                      # Заполните после регистрации бота

DADATA_API_KEY=ваш_реальный_ключ
PERPLEXITY_API_KEY=ваш_реальный_ключ
OPENROUTER_API_KEY=ваш_реальный_ключ

DEFAULT_MODEL=anthropic/claude-3.5-sonnet
OUR_PRODUCT_DESCRIPTION=Ваше описание продукта
```

Сохраните (Ctrl+X, Y, Enter) и перезапустите сервис:

```bash
systemctl restart sales-scout.service
```

### Шаг 3: Зарегистрируйте бота в Битрикс24

На локальной машине откройте `register_bot.py` и заполните:

```python
BITRIX24_WEBHOOK_URL = "https://ваш-портал.bitrix24.ru/rest/1/ваш_код"
```

Запустите:

```bash
python register_bot.py
```

Скопируйте полученный BOT_ID и добавьте в .env на сервере:

```bash
ssh myserver "nano /opt/sales-scout/.env"
# Установите BITRIX24_BOT_ID=полученный_id
# Ctrl+X, Y, Enter

ssh myserver "systemctl restart sales-scout.service"
```

### Шаг 4: Протестируйте

1. Откройте Битрикс24
2. Найдите бота "Sales Scout"
3. Отправьте тестовый ИНН: `7707083893` (Яндекс)
4. Дождитесь досье (30-60 секунд)

## Проверка работоспособности

```bash
# Проверка что сервис запущен
ssh myserver systemctl status sales-scout.service

# Проверка API
curl http://aistudy.dev.o2it.ru:8100/health

# Просмотр логов в реальном времени
ssh myserver tail -f /opt/sales-scout/sales_scout.log
```

## Управление сервисом

```bash
# Перезапуск после изменения .env
ssh myserver systemctl restart sales-scout.service

# Остановка
ssh myserver systemctl stop sales-scout.service

# Запуск
ssh myserver systemctl start sales-scout.service

# Статус
ssh myserver systemctl status sales-scout.service
```

## Webhook URL для Битрикс24

При регистрации бота используйте:

```
http://aistudy.dev.o2it.ru:8100/webhook/bitrix
```

## Статистика

Просмотр статистики оценок:

```bash
curl http://aistudy.dev.o2it.ru:8100/stats
```

## Важные файлы на сервере

- **Код**: `/opt/sales-scout/`
- **Конфигурация**: `/opt/sales-scout/.env`
- **Логи**: `/opt/sales-scout/sales_scout.log`
- **Оценки**: `/opt/sales-scout/feedback_log.jsonl`
- **Systemd**: `/etc/systemd/system/sales-scout.service`

## Безопасность

Приложение полностью изолировано:
- ✅ Отдельная директория
- ✅ Отдельное виртуальное окружение
- ✅ Отдельный порт (8100)
- ✅ Отдельный systemd сервис
- ✅ Не влияет на другие проекты на сервере

## Стоимость работы

На 100 компаний/месяц:
- DaData: 0₽ (бесплатный лимит)
- Perplexity: ~1,000₽
- OpenRouter: ~500₽
- **Итого**: ~1,500₽/месяц

## Что дальше?

1. Заполните API ключи
2. Зарегистрируйте бота
3. Протестируйте на 5-10 компаниях
4. Соберите feedback от менеджеров
5. Улучшите промпты на основе feedback
6. Масштабируйте!

---

**Приложение готово к использованию!** 🚀

Подробная документация: `README.md` и `DEPLOYMENT.md`
