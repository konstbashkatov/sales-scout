# Sales Scout MVP

Автоматическое создание досье компаний для менеджеров по продажам.

## Описание

Sales Scout - это чат-бот для Битрикс24, который автоматически собирает информацию о компаниях по ИНН и создает подробное досье с рекомендациями по тактике продаж.

### Что умеет бот:

- Получает базовые данные о компании из ЕГРЮЛ (DaData)
- Находит официальный сайт и социальные сети (Perplexity)
- Извлекает контакты с сайта (телефоны, email)
- Ищет информацию о руководителях (ЛПР)
- Собирает бизнес-информацию (оборот, сотрудники, технологии)
- Генерирует досье с рекомендациями по тактике продаж (LLM)

## Архитектура

```
DaData API         → Базовые данные ЕГРЮЛ
Perplexity API     → Поиск в интернете (сайт, ЛПР, новости)
Website Parser     → Извлечение контактов
LangChain + LLM    → Анализ и рекомендации
Битрикс24 Bot      → Интерфейс для пользователя
```

## Требования

- Python 3.10+
- API ключи:
  - DaData API key
  - Perplexity API key
  - OpenRouter API key
  - Битрикс24 webhook URL

## Установка

### 1. Клонирование репозитория

```bash
cd "Sales scout"
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# macOS/Linux
source venv/bin/activate

# Windows
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Откройте `.env` и заполните:

```env
# Bitrix24
BITRIX24_WEBHOOK_URL=https://ваш-портал.bitrix24.ru/rest/1/ваш_код
BITRIX24_BOT_ID=

# DaData (регистрация: https://dadata.ru/)
DADATA_API_KEY=ваш_ключ

# Perplexity (регистрация: https://www.perplexity.ai/)
PERPLEXITY_API_KEY=ваш_ключ

# OpenRouter (регистрация: https://openrouter.ai/)
OPENROUTER_API_KEY=ваш_ключ

# Настройки
DEFAULT_MODEL=anthropic/claude-3.5-sonnet
OUR_PRODUCT_DESCRIPTION=Ваше описание продукта для рекомендаций
```

## Получение API ключей

### DaData

1. Зарегистрируйтесь на https://dadata.ru/
2. Бесплатный тариф: 10,000 запросов/день
3. API ключ в личном кабинете

### Perplexity

1. Зарегистрируйтесь на https://www.perplexity.ai/
2. Перейдите в dashboard.perplexity.ai
3. Создайте API ключ
4. Тарифы: https://docs.perplexity.ai/docs/pricing

### OpenRouter

1. Зарегистрируйтесь на https://openrouter.ai/
2. Пополните баланс ($5-10 для старта)
3. API ключ в настройках аккаунта

### Битрикс24

#### Вариант 1: Входящий webhook (проще)

1. В Битрикс24 → Приложения → Вебхуки
2. Создать вебхук
3. Включить права: `imbot` (все права для работы с ботами)
4. Скопировать URL вебхука

#### Вариант 2: Регистрация бота (рекомендуется)

Создайте файл `register_bot.py`:

```python
import requests
from dotenv import load_dotenv
import os

load_dotenv()

webhook_url = os.getenv("BITRIX24_WEBHOOK_URL")
ngrok_url = "https://ваш-ngrok-url.ngrok.io"  # Замените на ваш URL

response = requests.post(f"{webhook_url}/imbot.register", json={
    "CODE": "sales_scout",
    "TYPE": "B",
    "EVENT_MESSAGE_ADD": f"{ngrok_url}/webhook/bitrix",
    "EVENT_WELCOME_MESSAGE": "Y",
    "PROPERTIES": {
        "NAME": "Sales Scout",
        "LAST_NAME": "Бот",
        "COLOR": "BLUE",
        "EMAIL": "bot@salesscout.ru"
    }
})

result = response.json()
print("BOT_ID:", result.get("result"))
print("\nДобавьте BOT_ID в .env файл!")
```

Запустите:

```bash
python register_bot.py
```

## Запуск

### Локальная разработка

```bash
# Запуск сервера
python -m app.main

# Или с помощью uvicorn
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен на `http://localhost:8000`

### Запуск с ngrok (для webhook от Битрикс24)

В другом терминале:

```bash
ngrok http 8000
```

Скопируйте HTTPS URL (например, `https://abc123.ngrok.io`) и используйте при регистрации бота.

## Использование

### Как пользоваться ботом

1. Найдите бота "Sales Scout" в Битрикс24
2. Отправьте ИНН компании (10 или 12 цифр)
3. Дождитесь досье (30-60 секунд)
4. Оцените результат с помощью кнопок

### Пример

```
Вы: 7707083893

Бот: 🔍 Ищу информацию о компании с ИНН 7707083893...
     Это может занять 30-60 секунд. Пожалуйста, подождите.

[Через 30-60 секунд]

Бот: [Подробное досье компании с рекомендациями]

     [👍 Полезно] [👎 Не полезно] [💬 Оставить отзыв]
```

## Endpoints API

- `GET /` - Информация о сервисе
- `GET /health` - Проверка здоровья
- `POST /webhook/bitrix` - Webhook для Битрикс24
- `GET /stats` - Статистика оценок

## Мониторинг

### Логи

Логи сохраняются в файл `sales_scout.log`:

```bash
tail -f sales_scout.log
```

### Статистика оценок

Просмотр статистики:

```bash
curl http://localhost:8000/stats
```

Оценки сохраняются в `feedback_log.jsonl`

## Структура проекта

```
sales_scout/
├── app/
│   ├── __init__.py
│   ├── main.py                  # FastAPI приложение
│   ├── config.py                # Конфигурация
│   ├── services/
│   │   ├── dadata.py           # DaData API
│   │   ├── perplexity.py       # Perplexity API
│   │   ├── website_parser.py   # Парсинг сайтов
│   │   ├── bitrix.py           # Битрикс24 API
│   │   └── sales_analyzer.py   # LangChain анализатор
│   └── webhooks/
│       └── bitrix_handler.py   # Обработчик webhook
├── .env                         # Переменные окружения
├── .env.example
├── requirements.txt
├── README.md
├── sales_scout.log             # Логи (создается автоматически)
└── feedback_log.jsonl          # Оценки (создается автоматически)
```

## Стоимость работы

Примерная стоимость на 100 компаний/месяц:

| Сервис | Стоимость |
|--------|-----------|
| DaData | 0₽ (бесплатный лимит) |
| Perplexity API | ~1,000₽ ($25-30) |
| OpenRouter (LLM) | ~500₽ |
| **Итого** | **~1,500₽/мес** |

## Troubleshooting

### Ошибка "Company not found"

- Проверьте правильность ИНН (10 или 12 цифр)
- Убедитесь что компания существует в ЕГРЮЛ

### Ошибка Perplexity API

- Проверьте API ключ
- Проверьте баланс на счете
- Проверьте лимиты запросов

### Бот не отвечает в Битрикс24

- Убедитесь что сервер запущен
- Проверьте ngrok URL (не истек ли срок)
- Проверьте логи: `tail -f sales_scout.log`
- Проверьте webhook URL в настройках бота

### Долгая обработка

- Perplexity API может работать 30-60 секунд
- Это нормально для качественного поиска
- Пользователь получает уведомление о процессе

## Разработка

### Запуск в режиме разработки

```bash
uvicorn app.main:app --reload
```

### Тестирование webhook локально

```bash
curl -X POST http://localhost:8000/webhook/bitrix \
  -H "Content-Type: application/json" \
  -d '{
    "event": "ONIMBOTMESSAGEADD",
    "data": {
      "MESSAGE": {
        "text": "7707083893",
        "chat_id": "123",
        "system": "N"
      }
    }
  }'
```

## Roadmap

### MVP (текущая версия) ✅
- DaData интеграция
- Perplexity поиск
- Базовый парсинг сайтов
- LLM анализ
- Битрикс24 бот

### v1.1 (планируется)
- Кэширование результатов (SQLite)
- Улучшенные промпты для LLM
- Расширенная статистика
- Dashboard для аналитики

### v2.0 (будущее)
- TenChat парсинг для ЛПР
- LinkedIn через Apify
- Email enrichment (Hunter.io)
- Больше источников данных

## Поддержка

При возникновении проблем:
1. Проверьте логи (`sales_scout.log`)
2. Проверьте все API ключи в `.env`
3. Убедитесь что ngrok запущен
4. Проверьте баланс на OpenRouter

## Лицензия

MIT

## Автор

Sales Scout MVP - 2025
