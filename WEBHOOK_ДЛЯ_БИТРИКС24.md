# 🚀 Webhook для автоматизаций Битрикс24

## ✅ Готовый URL (скопируйте и вставьте):

```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Document:TITLE}}&inn={{=Document:UF_CRM_INN}}&dealTitle={{=Document:DEAL_TITLE}}&companyWebsite={{=Document:UF_CRM_WEBSITE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

**Все параметры опциональные кроме userId!**

---

## 📝 Доступные параметры:

| Параметр | Описание | Обязательный | Пример Битрикс24 |
|----------|----------|--------------|------------------|
| `userId` | ID пользователя | **Да** | `{{=Document:ASSIGNED_BY_ID}}` |
| `companyName` | Название компании | Нет | `{{=Document:TITLE}}` или `{{=Document:COMPANY_TITLE}}` |
| `inn` | ИНН компании | Нет | `{{=Document:UF_CRM_INN}}` |
| `dealTitle` | Название сделки | Нет | `{{=Document:DEAL_TITLE}}` |
| `companyWebsite` | Сайт компании | Нет | `{{=Document:UF_CRM_WEBSITE}}` |

**Должен быть указан хотя бы один:** `companyName`, `dealTitle` или `inn`

---

## 📝 Варианты использования:

### Вариант 1: Полный (все параметры)

```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Document:COMPANY_TITLE}}&inn={{=Document:UF_CRM_INN}}&dealTitle={{=Document:DEAL_TITLE}}&companyWebsite={{=Document:UF_CRM_WEBSITE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

### Вариант 2: Минимальный (только название)

```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Document:TITLE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

### Вариант 3: Только ИНН

```
http://aistudy.dev.o2it.ru:8100/webhook/research?inn={{=Document:UF_CRM_INN}}&userId={{=Document:ASSIGNED_BY_ID}}
```

### Вариант 4: Название сделки (если компания не заполнена)

```
http://aistudy.dev.o2it.ru:8100/webhook/research?dealTitle={{=Document:DEAL_TITLE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

---

## 🔧 Настройка в Битрикс24:

### Автоматизация CRM (Роботы):

1. **CRM → Сделки → Настроить → Автоматизация**
2. Добавить робота: **"Webhook"**
3. **URL:** вставьте одну из строк выше
4. **Метод:** GET (или оставьте по умолчанию)
5. **Сохраните**

**Готово!** Теперь при срабатывании - ответственный получит досье в чат.

### Пример: При создании сделки

**Триггер:** Сделка создана
**Действие:** Webhook
**URL:**
```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Document:COMPANY_TITLE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

**Результат:** Менеджер получает досье компании сразу после создания сделки!

---

## 🎯 Параметры:

| Параметр | Что указать в Битрикс24 | Пример |
|----------|-------------------------|--------|
| `companyName` | `{{=Document:TITLE}}` или `{{=Document:COMPANY_TITLE}}` | Яндекс |
| `inn` | `{{=Document:UF_CRM_INN}}` или другое поле с ИНН | 7707083893 |
| `userId` | `{{=Document:ASSIGNED_BY_ID}}` - ответственный | 10 |

**Обязательный:** `userId`
**Опционально:** `companyName` или `inn` (хотя бы один)

---

## ✅ Примеры для разных сущностей:

### Сделка (Deal):
```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Document:COMPANY_TITLE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

### Контакт (Contact):
```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Contact:COMPANY_TITLE}}&userId={{=Contact:ASSIGNED_BY_ID}}
```

### Компания (Company):
```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Company:TITLE}}&inn={{=Company:UF_CRM_INN}}&userId={{=CurrentUser:ID}}
```

### Лид (Lead):
```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Lead:COMPANY_TITLE}}&userId={{=Lead:ASSIGNED_BY_ID}}
```

---

## 🧪 Тестирование:

### Тест в браузере (откройте URL):

```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName=Яндекс&userId=10
```

**Должны увидеть:**
```json
{"status":"ok","message":"Исследование компании 'Яндекс' запущено"}
```

**И через 1-2 минуты** пользователю 10 придет досье!

### Тест с curl:

```bash
curl "http://aistudy.dev.o2it.ru:8100/webhook/research?companyName=Сбербанк&userId=10"
```

---

## 💡 Сценарии использования:

### 1. Автоисследование при создании сделки

**Где:** CRM → Сделки → Автоматизация → Стадия "Новая"
**Робот:** Webhook
**URL:**
```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Document:COMPANY_TITLE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

**Эффект:** Менеджер создал сделку → автоматически получает досье компании

### 2. Исследование перед звонком

**Где:** CRM → Сделки → Автоматизация → Стадия "Подготовка к звонку"
**Робот:** Webhook
**URL:** как выше

**Эффект:** Перед звонком менеджер автоматически получает актуальное досье

### 3. Кнопка "Исследовать компанию"

**Где:** CRM → Настройка карточки → Кнопка
**Действие:** Вызвать бизнес-процесс
**В БП:** Webhook (URL как выше)

**Эффект:** Менеджер нажал кнопку → получил досье

---

## 📊 Что получит пользователь:

1. **Моментально:** "✅ Запрос получен, формирую досье..."
2. **Через 1-2 минуты:** Полное досье компании в чате
3. **Кнопки:** Оценить результат

---

## 🎯 Копируйте эту строку:

### Для сделок:
```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Document:COMPANY_TITLE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

### Для контактов:
```
http://aistudy.dev.o2it.ru:8100/webhook/research?companyName={{=Document:TITLE}}&userId={{=Document:ASSIGNED_BY_ID}}
```

**Метод:** GET
**Никаких дополнительных настроек не нужно!**

---

Готово к использованию! 🎉
